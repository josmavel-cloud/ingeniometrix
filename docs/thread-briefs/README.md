# Thread Briefs

Store one short markdown file per completed thread.

Purpose:

- preserve durable decisions
- avoid re-reading old chat history
- keep future prompts short

Suggested file name:

`YYYYMMDD-IMX-<thread-name>.md`

Use `_template.md` as the starting point.
